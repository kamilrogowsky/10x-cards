// import { http, HttpResponse } from "msw";

export const handlers = [
  // Mock API handlers will be added here as needed
  // Example:
  // http.get('/api/example', () => {
  //   return HttpResponse.json({ message: 'Mocked response' });
  // }),
];
